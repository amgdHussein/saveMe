import 'package:flutter/cupertino.dart';

class ReportFinding extends StatelessWidget {
  const ReportFinding({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Report Finding"),
      ),
    );
  }
}